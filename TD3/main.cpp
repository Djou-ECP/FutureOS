#include "td-3-a.h"
#include "td-3-b.h"
#include "td-3-c.h"

#include <iostream>

int main(int argc, char const *argv[]) {
  Calibrator calibrator(1000, 10);
  /*
  CountDown cd(10);
  cd.start(1000);
  while (cd.counter >= 0) {
  }
  */
  return 0;
}
